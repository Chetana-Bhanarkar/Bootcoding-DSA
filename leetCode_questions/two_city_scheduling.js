const parentArray = [[10,20],[30,200],[400,50],[30,20]]
  
let sum = 0 ; min ; 
  parentArray.map(
    childArray => childArray.map(
        arrayItem => ({
            const min =  Math.min(...arrayItem)
             sum += min  ;
        }
             
        )
      )
    )
  


   